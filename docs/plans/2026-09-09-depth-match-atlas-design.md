# 深度匹配报告：方向对照、筛选排序与公式

第 4 节展示全部 601 日的结果，Buy、Sell 两张 Rank IC 图按相同的 12 类定义对齐，横轴范围一致。查看方式选择买卖两侧或单侧，同时更新图表、分期表、定义配对表和相关矩阵。24 列全样本的协方差有效秩另作明确说明，不随方向筛选重新计算。

第 5 节统一筛选、排序结果表、导航和详情。研究逻辑分为数量与深度匹配、价格约束与执行行为、盘口变化与相邻订单、近期权重与时间分布。各类下保留正式定义，再展开 Buy、Sell。标题选择其范围，箭头独立展开或收起。

“保留因子分组”保留研究逻辑和定义分组，同一定义的 Buy、Sell 相邻；“不分组 · 全部混排”将每个方向因子独立排序。排序依据包括编号、IC 数值、绝对 IC、Rank IC 数值、绝对 Rank IC 和绝对 G10−G1。表格、导航与 CSV 使用相同筛选、评价区间和排列顺序。导航同时显示带符号的 IC、Rank IC，顺序与详情一致。

选中定义时，桌面并排显示 Buy、Sell 的完整详情；小屏按上下顺序展示。点击方向只显示该方向因子。选择逻辑标题显示该组全部符合筛选条件的因子。原选择不再符合筛选时，选择匹配项并提示；无匹配项时清空详情并禁用表格下载。

所有详情同步标签页、评价区间及日度明细日期。评价区间影响汇总指标、导航数值和分组诊断；日度图、月度图、分布和 OLS 保持全历史。日期筛选只影响日度明细和 CSV，累计列保留全历史起点的累计值。日期倒置时提示并禁用下载。图片、SVG、OLS JSON、分组 CSV、日度 CSV 和 notebook 均绑定具体正式列与方向。

URL 保存方向范围、分组方式、排序、搜索、研究逻辑、定义筛选、选择范围、评价区间、标签页和明细日期，刷新后恢复。

数学表达式采用内嵌 MathML，分数上下排列。dm01—dm11 展示单笔已观察订单得分，再按侧求和并除以双边有效订单总数 N。dm12 直接展示包含 C/N 的日度熵因子，不重复归一化；unknown、零值及缺失规则保留。原始登记表达式保留在各定义的折叠区。

生成器从当前已验收的报告数据渲染展示版和单文件离线版。离线版内嵌全部 120 张展示图片；notebook、评价副本与证据另由三个完整阅读包分卷提供。单文件离线版独立下载。24 张日度图及对应 SVG 使用既有日度序列生成，标签统一为 IC。封存来源文件和数值不变。

验证包括 6 项浏览器场景和 5 项数据、图片及阅读包检查。覆盖分组及混排、范围选择与折叠、全部方向指标、日期与区间、各类下载、公式实际排版、120 张离线图片解码、移动端及页面状态恢复。展示版与离线版数据 SHA-256 均保持 `348d4dd6f898d671b0973dac9a5d8265ccc70e2c71191e77cf402e02d8924dfe`。
